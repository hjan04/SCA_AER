# SCA_AER 1차 보고서 초안 구성

> 이 문서는 뼈대(outline)입니다. 시뮬레이션/합성 진행하면서 각 섹션의
> 실제 수치, 스크린샷, 리포트 내용을 채워 넣으시면 됩니다.

---

## 0. 표지
- 대회명 / 팀명(SCA) / 팀원 명단
- 제출일 (2026-08-28)
- 주제: Bio-mimic Neuron을 위한 AER 통신방식 설계

## 1. 서론

### 1.1 연구 배경
뉴로모픽(neuromorphic) 시스템은 생물학적 신경망의 동작 방식을 모사하여, 연속적인 신호 대신 이산적인 스파이크(spike) 형태로 정보를 주고받는다. 이때 다수의 뉴런이 발생시키는 스파이크를 제한된 통신 채널로 효율적으로 전달하기 위한 방식이 AER(Address Event Representation)이다. AER은 스파이크가 발생한 뉴런의 "주소"만을 전송함으로써 배선 수를 줄이고, 시간 다중화(time-division multiplexing)를 통해 다수의 가상 채널을 하나의 물리적 채널로 통합한다는 장점이 있다. 그러나 이 방식은 태생적으로 비동기(asynchronous) 통신을 전제로 설계되어 왔으며, 이는 표준 동기식 디지털 설계 플로우(RTL → Synthesis → STA)와 궁합이 맞지 않는 구조적 한계를 갖는다.

### 1.2 연구 목적
본 연구는 전통적인 AER 통신 방식을 분석하여 그 구조적 문제점을 도출하고, 이를 개선한 AER 설계 방향을 제시한 뒤 실제로 RTL 수준에서 구현하는 것을 목표로 한다. 특히 본 대회의 제출 요건(RTL, Synthesis, Timing 최적화, Area, Power, 최대 동작 주파수)을 고려하여, 개선 방향은 정량적으로 합성·검증 가능한 형태로 구체화한다.

### 1.3 진행 방식 요약
전통 AER과 개선 AER을 각각 별도의 RTL로 구현하고, 동일한 이벤트 소스 수(N)와 가능한 한 동일한 인터페이스를 유지하여 두 설계를 공정하게 비교한다. 두 설계 모두 Xcelium을 통한 기능 시뮬레이션으로 정상 동작을 검증한 후, Genus를 통해 합성하고, 동일한 조건에서 Area/Power/Timing을 측정하여 최대 동작 주파수를 산출한다. 최종적으로 두 구조의 성능 지표를 종합 비교하여 개선의 효과를 정량적으로 제시한다.

## 2. 전통 AER 분석
- 2.1 AER 통신 방식 개요 (4-phase REQ/ACK 핸드셰이크, 아비트레이션 개념)
- 2.2 전통 AER의 구조적 특징
- 2.3 문제점 도출
  - 고정 우선순위 아비터의 starvation 문제
  - 비동기 특성으로 인한 표준 동기식 EDA 플로우와의 부정합
  - (필요시) 확장성/버스 병목 문제
- 2.4 참고 문헌 및 레퍼런스 설계 (예: ODIN/tinyODIN 구조 참고 — 인용 명시)

## 3. 개선 AER 설계 방향
- 3.1 문제점별 개선 아이디어 정리
- 3.2 최종 채택한 개선 방향과 근거
- 3.3 개선 구조 개요도 (블록 다이어그램)

## 4. RTL 설계
- 4.1 전통 AER RTL
  - 모듈 구성 (aer_tx_traditional, aer_rx_traditional)
  - 포트/인터페이스 정의
  - 핵심 로직 설명 (아비터, FSM 핸드셰이크)
- 4.2 개선 AER RTL
  - 모듈 구성 및 변경점
  - 핵심 로직 설명
- 4.3 두 설계 간 인터페이스 일관성 (공정 비교를 위한 설계 원칙)

## 5. 기능 검증 (Functional Simulation)
- 5.1 Testbench 구성 및 검증 시나리오
  - 단일 이벤트 케이스
  - 동시 이벤트(충돌) 케이스
- 5.2 Xcelium 시뮬레이션 결과
  - 콘솔 로그 / 파형 스크린샷
  - 전통 vs 개선 구조에서 이벤트 처리 순서 비교

## 6. Synthesis (Genus)
- 6.1 합성 환경 (표준셀 라이브러리, 툴 버전)
- 6.2 초기 타이밍 제약(SDC) 조건
- 6.3 합성 결과 요약 (게이트 수, 셀 종류 분포)

## 7. Timing 분석 및 최적화
- 7.1 초기 타이밍 리포트 (critical path, slack)
- 7.2 최적화 과정 (클록 주기 조정 반복, 구조적 개선 내역)
- 7.3 최종 타이밍 결과

## 8. Area 분석
- 8.1 report_area 결과 (전통 / 개선 각각)
- 8.2 면적 차이 원인 분석

## 9. Power 분석
- 9.1 report_power 결과 (전통 / 개선 각각)
- 9.2 소비전력 차이 원인 분석

## 10. 최대 동작 주파수 (Fmax) 산출
- 10.1 Fmax 탐색 방법 (클록 주기를 좁혀가며 slack=0에 근접하는 지점 탐색)
- 10.2 전통 / 개선 구조 각각의 최종 Fmax

## 11. 기존 구조 vs 제안 구조 종합 비교
| 항목 | 전통 AER | 개선 AER | 개선율 |
|---|---|---|---|
| Area (mm²) | | | |
| Power (mW) | | | |
| Fmax (MHz) | | | |
| Critical path (ns) | | | |
| (선택) 이벤트 처리 지연 | | | |

- 11.1 비교 결과 해석
- 11.2 개선 구조가 유의미한 이유 (수치 기반 논거)

## 12. 결론 및 2차 과제 연계 방향
- 12.1 1차 결과 요약
- 12.2 한계점 및 개선 여지
- 12.3 2차 과제(n×m 시각 정보 → N×M 메모리 매핑)와의 연결점

## 13. 참고문헌
- Boahen, K. (2000) — AER 리뷰 및 역사
- Frenkel, C. et al. (2019), tinyODIN/ODIN HDL 소스 참고
- (기타 인용 자료)

## 부록
- A. 전체 RTL 코드
- B. Genus/Xcelium 스크립트 (filelist, tcl, sdc)
- C. 원본 리포트 파일 (report_area.rpt 등 전문)
